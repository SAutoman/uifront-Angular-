// import { createSelector } from '@ngrx/store';
// import { ProcessStepState } from './process-step.reducer';

// export const selectProcessStepState: (p: any) => ProcessStepState = (state) => state.processSteps;

// export const processStepSelector = createSelector(selectProcessStepState, (step) => step.data);

// export const processStepData = createSelector(selectProcessStepState, (data) => data.entities);
