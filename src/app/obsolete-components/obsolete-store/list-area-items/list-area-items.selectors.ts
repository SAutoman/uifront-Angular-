// import { createSelector } from '@ngrx/store';
// import { ListAreaItemsState } from '../list-area-items/list-area-items.reducer';

// export const SelectListAreaState: (u: any) => ListAreaItemsState = (state) => state.listAreaItemsState;

// export const getListAreaItemsSelector = createSelector(SelectListAreaState, (ListAreaItems) => ListAreaItems.ListAreaItems);
