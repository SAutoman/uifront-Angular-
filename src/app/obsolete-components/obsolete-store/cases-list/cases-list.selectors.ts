// import { createSelector, createFeatureSelector } from '@ngrx/store';
// import { CasesListState } from './cases-list.reducer';

// const getUserSettings = createFeatureSelector<CasesListState>('casesList');

// // not used
// export const getLayoutSelector = createSelector(getUserSettings, (casesList) => (casesList ? casesList.layout : null));

// // export const getUserSettingsLayoutsSelector = createSelector(getUserSettings, (layouts) =>
// //   layouts.userSettings.filter((x) => x.key === appCaseViewLayout)
// // );
