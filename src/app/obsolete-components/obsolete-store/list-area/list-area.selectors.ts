// import { createSelector } from '@ngrx/store';
// import { ListAreaState } from './list-area.reducer';

// export const SelectListAreaState: (u: any) => ListAreaState = (state) => state.listAreaState;

// export const getListAreaSelector = createSelector(SelectListAreaState, (ListArea) => ListArea.ListArea);
